import 'package:flutter/material.dart';

class CustomColors {
  static const Color background = Color(0xffF9F6E6); // Beyaz arka plan
  static const Color button = Color(0xFFDAA520); // Koyu yeşil buton rengi
  static const Color buttontext = Color(0xFFFFFFFF); // buton text rengi
  static const Color textfieldFill = Color(0xFFF5D76E); // Açık yeşil input arka plan
  static const Color iconColor = Color(0xFF1D4B0B); // Ikon rengi
  static const Color primaryText = Color(0xFF1D4B0B); // Başlık yazısı ve diğer koyu metinler
  static const Color hintText = Color(0xFF8B5E3B); // Placeholder rengi
  static const Color error = Colors.redAccent; // Snackbar arka plan rengi (hata)
  static const Color lightText = Color(0xFFFFF8E1); // Daha açık metinler için
}

